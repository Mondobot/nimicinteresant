MVC:
	- Model:
		- Users(Name + UserID) -cache
		- Files(Name + FileID + UserID) -cache
		- Transfers(File + UserS + UserD + TransfID + Progress) -cache
		- myUser

		- SetUsers(lista Users)
		- SetTransfers(lista Transfers)
		- SetFiles(lista Files)
		- GetUid(name)
	
		// Doar pt View
		- GetFiles()
		- GetTransfers()
		- GetUsers()

	- Controller:
		// Pt_View
		- UpdateUsers()
		- UpdateFiles()
		- UpdateTransfers()

	- View:
		- DisplayUsers()
		- DisplayFiles()
		- DisplayTransfers()

	- Mediator:
		- GetFiles(UserID / name)
		- GetTransfers(UserID / name)
		- GetUsers(my_UserID)
